import "whatwg-fetch";
import getHistory from "react-router-global-history";

import Config from "Config";

// const setActor = actor => ({
//     type: 'SET_ACTOR',
//     actor: actor
// });

let Actions = {
  // setToolShowedInModal(tool) {
  //     return (dispatch, getState) => {
  //         let toolShowedInModal = tool ? { key: tool.key, name: tool.name, ref: tool.ref } : null
  //         return dispatch(filterHistoryByTool(toolShowedInModal));
  //     }
  // },
};

export default Actions;
